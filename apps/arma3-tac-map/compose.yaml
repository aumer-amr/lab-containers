services:
  tacmap:
    build: .
    image: ghcr.io/aumer-amr/arma3-tac-map:local
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      PUBLIC_URL: ${PUBLIC_URL:-http://localhost:8080}
      DISCORD_CLIENT_ID: ${DISCORD_CLIENT_ID}
      DISCORD_CLIENT_SECRET: ${DISCORD_CLIENT_SECRET}
      DISCORD_GUILD_ID: ${DISCORD_GUILD_ID}
      DISCORD_ALLOWED_ROLE_ID: ${DISCORD_ALLOWED_ROLE_ID}
      ADMIN_DISCORD_USER_IDS: ${ADMIN_DISCORD_USER_IDS:-}
      MAP_UPLOAD_MAX_BYTES: ${MAP_UPLOAD_MAX_BYTES:-5368709120}
    volumes:
      - ./data:/data
      - ./maps:/maps
      - ./tmp:/tmp
    read_only: true
